import type { Namespace } from '@/types';

export const namespace: Namespace = {
    name: 'X (Twitter)',
    url: 'x.com',
    description: 'A lightweight Twitter/X RSS template with user timeline, media timeline, single tweet, and keyword search routes.',
    lang: 'en',
};
